# The @nikhpf scope is published to / installed from GitHub Packages.
# Auth is NOT stored here — provide a token with the right scope in your own
# ~/.npmrc (read:packages to install, write:packages to publish):
#   //npm.pkg.github.com/:_authToken=YOUR_GITHUB_PAT
@nikhpf:registry=https://npm.pkg.github.com
